name = "gantt-app"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Cloudflare Pages doesn't use these, but they're here for reference
# The actual configuration is done in the Cloudflare dashboard
